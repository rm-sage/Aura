# pnpm 11 errors out in non-interactive shells when a dependency has
# install scripts that haven't been explicitly approved. The Aura repo
# only has one such dep (@sentry/cli), and it ships its binary lazily
# on first invocation, so blocking install scripts globally is safe
# and lets CI / hook environments run pnpm install non-interactively.
ignore-scripts=true
